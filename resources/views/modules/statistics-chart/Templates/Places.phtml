<?php

/**
 * This file is part of the package magicsunday/webtrees-statistics.
 *
 * For the full copyright and license information, please read the
 * LICENSE file distributed with this source code.
 */

declare(strict_types=1);

use Fisharebest\Webtrees\I18N;
use MagicSunday\Webtrees\Statistic\Statistic;

/**
 * @var string    $module    The name of the module
 * @var Statistic $statistic
 */
$migrationFlows = $statistic->getMigrationFlows(20);

$sankeyExplanation = I18N::translate(
    'Each flow shows individuals who were born in one country and died in another. The flow\'s thickness encodes the count: thicker bands mean more migrations along that path. Same-country trajectories are excluded — they\'re not migration. Hover any band to see the exact source → destination and individual count.'
);

$placeDispersion = $statistic->getPlaceDispersionSummary();
?>
<div class="container">
    <div class="row row-cols-1 row-cols-md-1 g-3">
        <div class="col" data-map-type="birth">
            <div class="card h-100">
                <?php
                    $birthsByCountry = $statistic->getBirthsByCountry();
$totalBirths                         = array_sum(array_column($birthsByCountry, 'count'));
?>
                <div class="card-header">
                    <h5 class="card-title"><?php echo I18N::translate('Place of birth'); ?></h5>
                    <small><?php echo I18N::plural('%s recorded country of birth', '%s recorded countries of birth', $totalBirths, I18N::number($totalBirths)); ?></small>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-3">
                            <?php
                $progressList = [];

foreach ($birthsByCountry as $values) {
    $progressList[$values['label']] = $values['count'];
}

echo view(
    $module . '::modules/statistics-chart/Partials/ProgressList',
    [
        'class' => 'progress-births-country',
        'data'  => array_slice($progressList, 0, 10),
    ]
);
?>
                        </div>
                        <div class="col-md-9 mt-3 mt-md-0">
                            <?php
    echo view(
        $module . '::modules/statistics-chart/Partials/GeoMap',
        [
            'identifier' => 'living-geo-map',
            'class'      => '',
            'data'       => $birthsByCountry,
        ]
    );
?>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col" data-map-type="residence">
            <div class="card h-100">
                <?php
                    $residencesByCountry = $statistic->getResidencesByCountry();
$totalResidences                         = array_sum(array_column($residencesByCountry, 'count'));
?>
                <div class="card-header">
                    <h5 class="card-title"><?php echo I18N::translate('Recorded residences'); ?></h5>
                    <small><?php echo I18N::plural('%s recorded residence', '%s recorded residences', $totalResidences, I18N::number($totalResidences)); ?></small>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-3">
                            <?php
                $progressList = [];

foreach ($residencesByCountry as $values) {
    $progressList[$values['label']] = $values['count'];
}

echo view(
    $module . '::modules/statistics-chart/Partials/ProgressList',
    [
        'class' => 'progress-residences-country',
        'data'  => array_slice($progressList, 0, 10),
    ]
);
?>
                        </div>
                        <div class="col-md-9 mt-3 mt-md-0">
                            <?php
    echo view(
        $module . '::modules/statistics-chart/Partials/GeoMap',
        [
            'identifier' => 'residences-geo-map',
            'class'      => '',
            'data'       => $residencesByCountry,
        ]
    );
?>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col" data-map-type="death">
            <div class="card h-100">
                <?php
                    $deathsByCountry = $statistic->getDeathsByCountry();
$totalDeaths                         = array_sum(array_column($deathsByCountry, 'count'));
?>
                <div class="card-header">
                    <h5 class="card-title"><?php echo I18N::translate('Place of death'); ?></h5>
                    <small><?php echo I18N::plural('%s recorded country of death', '%s recorded countries of death', $totalDeaths, I18N::number($totalDeaths)); ?></small>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-3">
                            <?php
                $progressList = [];

foreach ($deathsByCountry as $values) {
    $progressList[$values['label']] = $values['count'];
}

echo view(
    $module . '::modules/statistics-chart/Partials/ProgressList',
    [
        'class' => 'progress-deaths-country',
        'data'  => array_slice($progressList, 0, 10),
    ]
);
?>
                        </div>
                        <div class="col-md-9 mt-3 mt-md-0">
                            <?php
    echo view(
        $module . '::modules/statistics-chart/Partials/GeoMap',
        [
            'identifier' => 'deaths-geo-map',
            'class'      => '',
            'data'       => $deathsByCountry,
        ]
    );
?>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col">
            <div class="card h-100">
                <div class="card-header">
                    <h5 class="card-title">
                        <?php echo I18N::translate('Migrations between birth and death country'); ?>
                        <button
                            type="button"
                            class="wt-card-info-button"
                            data-bs-toggle="popover"
                            data-bs-trigger="hover focus"
                            data-bs-placement="top"
                            data-bs-html="false"
                            data-bs-title="<?php echo e(I18N::translate('About this chart')); ?>"
                            data-bs-content="<?php echo e($sankeyExplanation); ?>"
                            aria-label="<?php echo e(I18N::translate('About this chart')); ?>"
                        >?</button>
                    </h5>
                    <small><?php echo I18N::plural('%s distinct migration path', '%s distinct migration paths', count($migrationFlows->links), I18N::number(count($migrationFlows->links))); ?></small>
                </div>
                <div class="card-body">
                    <?php
        echo view(
            $module . '::modules/statistics-chart/Partials/SankeyFlow',
            [
                'identifier' => 'migration-sankey',
                'data'       => $migrationFlows,
            ]
        );
?>
                </div>
            </div>
        </div>
    </div>

    <?php if ($placeDispersion['sampled'] > 0) { ?>
    <?php
    $placeDispersionExplanation = I18N::translate(
        'For every individual the widget collects every distinct PLAC value that appears on any of their level-1 events (BIRT, DEAT, BAPM, BURI, RESI, …) and counts the unique places. Duplicates within the same individual (e.g. BIRT and DEAT both in Hamburg) collapse to one place, so the count reflects geographic mobility rather than how many events are recorded. The average is across every individual with at least one PLAC sample; individuals with no place data at all are excluded. The distribution on the right shows how those counts split across 1 / 2 / 3 / 4 / 5+ distinct places per person — a high tail signals migrant ancestors, a heavy bar at "1" signals a sedentary or sparsely-documented population.'
    );
        ?>
    <div class="row row-cols-1 row-cols-md-2 g-3">
        <div class="col">
            <div class="card h-100">
                <div class="card-header">
                    <h5 class="card-title">
                        <?php echo I18N::translate('Geographic dispersion'); ?>
                        <button
                            type="button"
                            class="wt-card-info-button"
                            data-bs-toggle="popover"
                            data-bs-trigger="hover focus"
                            data-bs-placement="top"
                            data-bs-html="false"
                            data-bs-custom-class="wt-popover-wide"
                            data-bs-title="<?php echo e(I18N::translate('About this metric')); ?>"
                            data-bs-content="<?php echo e($placeDispersionExplanation); ?>"
                            aria-label="<?php echo e(I18N::translate('About this metric')); ?>"
                        >?</button>
                    </h5>
                    <small><?php echo I18N::translate('Average count of distinct PLAC values per individual across BIRT, DEAT, BAPM, BURI, RESI and similar level-1 events.'); ?></small>
                </div>
                <div class="card-body d-flex align-items-center justify-content-center">
                    <?php
            echo view(
                $module . '::modules/statistics-chart/Partials/Scalar',
                [
                    'value'   => I18N::number($placeDispersion['average'], 2),
                    'unit'    => I18N::translate('places'),
                    'caption' => I18N::plural(
                        'Across %s individual with at least one recorded place.',
                        'Across %s individuals with at least one recorded place.',
                        $placeDispersion['sampled'],
                        I18N::number($placeDispersion['sampled']),
                    ),
                ],
            );
        ?>
                </div>
            </div>
        </div>
        <div class="col">
            <div class="card h-100">
                <div class="card-header">
                    <h5 class="card-title"><?php echo I18N::translate('Distinct places per individual'); ?></h5>
                    <small><?php echo I18N::translate('How many distinct places each individual carries, bucketed.'); ?></small>
                </div>
                <div class="card-body">
                    <?php
                echo view(
                    $module . '::modules/statistics-chart/Partials/ProgressList',
                    [
                        'class' => 'progress-place-dispersion',
                        'data'  => $placeDispersion['distribution'],
                    ],
                );
        ?>
                </div>
            </div>
        </div>
    </div>
    <?php } ?>
</div>
