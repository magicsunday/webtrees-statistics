name: Greetings

on:
    issues:
        types: [ opened ]
    pull_request_target:
        types: [ opened ]

permissions:
    contents: read

jobs:
    greeting:
        uses: magicsunday/.github/.github/workflows/greetings.yml@main
        permissions:
            issues: write
            pull-requests: write
